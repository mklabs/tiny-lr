{
  "name": "grunt-example",
  "version": "0.0.0",
  "description": "Example running the plugin on grunt 0.3 / 0.4",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "version": "grunt --version",
    "help": "grunt --help",
    "start": "grunt server reload --verbose"
  }
}
